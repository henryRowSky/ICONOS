var genero = `Masculino`;

if (genero == 'Masculino') {
    console.log('El baño esta a la derecha');
} else {
    console.log('El baño esta a la izquierda');
}
var edad = prompt();
edad = parseInt(edad);
if (edad >= 18) {
    console.log(`Tienes ${edad} eres mayor de edad`);
} else {
    console.log(`Tienes ${edad} eres menor de edad`);
}

// Igualdad (==) 	
// Devuelve true si ambos operadorandos son iguales.
// Desigualdad (!=) 	
// Devuelve true si ambos operandos no son iguales.
// Estrictamente iguales (===) 	
// Devuelve true si los operandos son iguales y tienen el mismo tipo. Mira también Object.is y sameness in JS.
// Estrictamente desiguales (!==) 	
// Devuelve true si los operandos no son iguales y/o no son del mismo tipo.
// Mayor que (>) 
// Devuelve true si el operando de la izquierda es mayor que el operando de la derecha.
// Mayor o igual que (>=) 	
// Devuelve true si el operando de la izquierda es mayor o igual que el operando de la derecha.
// Menor que (<) 	
// Devuelve true si el operando de la izquierda es menor que el operando de la derecha.
// Menor o igual que (<=) 	
// Devuelve true si el operando de la izquierda es menor o igual que el operando de la derecha.